# AI Agent Instructions

本仓库主要由大模型编程工具维护。每次新会话必须先按下面规则建立上下文，避免读取过多无关文档。

## 启动协议

1. 先读取本文件。
2. 再读取 `docs/INDEX.md`。
3. 根据 `docs/INDEX.md` 的任务路由表，只读取当前任务需要的模块文档。
4. 不要默认读取整个 `docs/` 目录，也不要默认读取所有历史规格。
5. 修改代码前，先确认当前任务涉及的规范文件，并按这些规范实现。
6. 修改功能、接口、数据模型、评分规则或外部数据接入后，同步更新对应文档。

## 项目核心约束

- 项目是“银河机位多地点对比 Web/PWA”，核心价值是多个候选地点同屏决策。
- 第一阶段按个人自用设计，不做社区、公开地点库、订阅和复杂权限。
- 不依赖第三方私有或未公开接口。
- 光污染数据必须区分 VIIRS 辐亮度、SQM、Bortle 估算，不能混用概念。
- 距离必须区分直线距离和驾车距离，路线服务失败时不能用直线距离冒充驾车距离。
- 所有外部数据展示来源、时间、可信度或过期状态。

## 编码和文档规范

- 文档使用中文，文件编码使用 UTF-8。
- 代码、字段名、接口路径、环境变量使用英文。
- 新增模块时先更新 `docs/INDEX.md` 的路由表。
- 文档要写给 AI 执行者看：明确输入、输出、默认决策、禁止事项和验收条件。

## 常用入口

- 总目录：`docs/INDEX.md`
- 产品范围：`docs/01-product-scope.md`
- 数据模型：`docs/03-data-model.md`
- API：`docs/06-api-contracts.md`
- 开发规范：`docs/08-implementation-standards.md`

